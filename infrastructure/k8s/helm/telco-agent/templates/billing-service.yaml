apiVersion: apps/v1
kind: Deployment
metadata:
  name: telco-billing-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "telco-agent.labels" . | nindent 4 }}
    app.kubernetes.io/name: billing-service
spec:
  replicas: {{ .Values.billingService.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: billing-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: billing-service
    spec:
      containers:
        - name: billing-service
          image: "{{ .Values.billingService.image.repository }}:{{ .Values.billingService.image.tag }}"
          imagePullPolicy: {{ .Values.billingService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.billingService.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: telco-agent-config
            - secretRef:
                name: telco-agent-secrets
          resources:
            {{- toYaml .Values.billingService.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.billingService.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.billingService.port }}
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: telco-billing-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "telco-agent.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.billingService.port }}
      targetPort: {{ .Values.billingService.port }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: billing-service
