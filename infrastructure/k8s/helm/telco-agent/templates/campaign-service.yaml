apiVersion: apps/v1
kind: Deployment
metadata:
  name: telco-campaign-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "telco-agent.labels" . | nindent 4 }}
    app.kubernetes.io/name: campaign-service
spec:
  replicas: {{ .Values.campaignService.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: campaign-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: campaign-service
    spec:
      containers:
        - name: campaign-service
          image: "{{ .Values.campaignService.image.repository }}:{{ .Values.campaignService.image.tag }}"
          imagePullPolicy: {{ .Values.campaignService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.campaignService.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: telco-agent-config
            - secretRef:
                name: telco-agent-secrets
          resources:
            {{- toYaml .Values.campaignService.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.campaignService.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.campaignService.port }}
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: telco-campaign-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "telco-agent.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.campaignService.port }}
      targetPort: {{ .Values.campaignService.port }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: campaign-service
